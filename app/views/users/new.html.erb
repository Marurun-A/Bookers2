<p id="notice"></p>
<h1>User</h1>
<%= form_with model: @user do |f| %>
  <h4>Name</h4>
  <%= f.text_field :name %>
  <h4>Image</h4>
  <%= f.file_field :profile_image, accept: "image/*" %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create User' %>
<% end %>